{
  "name": "prds-marketing",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/verify-build.mjs",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test",
    "preview:offline": "node scripts/render-preview.mjs"
  },
  "dependencies": { "astro": "7.3.3" },
  "engines": { "node": ">=22.12.0" }
}
